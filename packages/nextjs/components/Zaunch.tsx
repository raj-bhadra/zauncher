"use client";

import { ZaunchForm } from "./helper/ZaunchForm";
import { Container } from "@mui/material";

export const Zaunch = () => {
  return (
    <Container maxWidth="sm">
      <ZaunchForm />
    </Container>
  );
};
